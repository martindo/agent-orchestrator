"""Tests for RetriableSearchService have been moved.

RetriableSearchService is domain-specific (research/fact-checking) and no
longer lives in the platform core.  The tests now live alongside the module:

    profiles/research-team/tests/test_retriable_search.py

To run them from the repo root:

    pytest profiles/research-team/tests/ -v
"""
